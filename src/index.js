import { init } from '/init.js';

document.addEventListener('DOMContentLoaded', () => init());

const OPERATORS = {
    plus: 'plus',
    minus: 'minus',
    multi: 'multi',
    divide: 'divide'
};
  
  
  
    
  