import { init } from './init';

document.addEventListener('DOMContentLoaded', () => init());

const OPERATORS = {
    plus: 'plus',
    minus: 'minus',
    multi: 'multi',
    divide: 'divide'
};
  
  
  
    
  